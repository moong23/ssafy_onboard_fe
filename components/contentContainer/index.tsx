"use clinet";
import { IContent } from "@/interface/content";
import { useState } from "react";
const ContentContainer = ({ props }: { props?: IContent }) => {
  return (
    <>
      <>CC</>
    </>
  );
};

export default ContentContainer;
